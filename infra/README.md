# Infrastructure

Config per deployment, CI/CD, containerizzazione.

- `infra/docker/` — Dockerfile per ogni servizio
- `infra/docker-compose/` — compose per sviluppo locale
- `infra/ci/` — pipeline CI/CD
- `infra/k8s/` — manifest Kubernetes (Phase 4)
